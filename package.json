{
  "devDependencies": {
    "@commitlint/config-conventional": "^19.0.0",
    "typescript": "^5.3.0"
  },
  "name": "github-meta",
  "private": true,
  "scripts": {
    "build": "npm run build --workspaces --if-present"
  },
  "workspaces": [
    "packages/*"
  ]
}
