{
  "devDependencies": {
    "typescript": "^5.3.0"
  },
  "name": "github-meta",
  "private": true,
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  },
  "scripts": {
    "build": "npm run build --workspaces --if-present"
  },
  "workspaces": [
    "packages/*"
  ]
}
